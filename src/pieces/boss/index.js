// PIECE: boss — placeholder. See src/pieces/registry.js for the brief.
import { makePlaceholder } from '../_shared/placeholder.js';
export async function create(ctx) { return makePlaceholder(ctx, 'boss'); }
